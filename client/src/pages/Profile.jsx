import { ProfileSection } from "../section"
import { ProfileBox, LogOut } from "../components"

const Profile = () => {
  return (
      <div>
          <ProfileSection />
          <ProfileBox />
          <LogOut />
    </div>
  )
}

export default Profile