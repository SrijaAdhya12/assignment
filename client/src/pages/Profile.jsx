import { ProfileSection } from "../section"
import { ProfileBox } from "../components"

const Profile = () => {
  return (
      <div>
          <ProfileSection />
          <ProfileBox />
    </div>
  )
}

export default Profile